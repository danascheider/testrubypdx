<%= form_for(@speaker) do |f| %>
  <% if @speaker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@speaker.errors.count, "error") %> prohibited this speaker from being saved:</h2>

      <ul>
      <% @speaker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <tr class="field">
      <th><%= f.label :first_name %></th>
      <td><%= f.text_field :first_name, class: 'form-control' %></td>
    </tr>
    <tr class="field">
      <th><%= f.label :last_name %></th>
      <td><%= f.text_field :last_name, class: 'form-control' %></td>
    </tr>
    <tr class="field">
      <th><%= f.label :website %></th>
      <td><%= f.text_field :website, class: 'form-control' %></td>
    </tr>
    <tr class="field">
      <th><%= f.label :company %></th>
      <td><%= f.text_field :company, class: 'form-control' %></td>
    </tr>
    <tr class="field">
      <th><%= f.label :bio %></th>
      <td><%= f.text_area :bio, class: 'form-control' %></td>
    </tr>
    <tr class="actions">
      <td colspan="2"><%= f.submit class: 'btn btn-success' %></td>
    </tr>
  </table>
<% end %>
