<p id="notice"><%= notice %></p>

<h1>Upcoming Meetings</h1>

<% if @meetings.size == 0 %>
  <p>No meetings have been scheduled yet! <a href='http://twitter.com/danascheider'>Follow @danascheider on Twitter</a> and you'll be the first to know when we announce one.</p>
<% else %>
  <table>
    <thead>
      <tr>
        <th>Date</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @meetings.each do |meeting| %>
        <tr>
          <td><%= meeting.date %></td>
          <td><%= link_to 'Show', meeting %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

<br>