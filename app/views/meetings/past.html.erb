<p id="notice"><%= notice %></p>

<h1>Past Meetings</h1>

<% if @meetings.size == 0 %>
  <p>We haven't had any meetings yet! Check out our <a href='/meetings/upcoming'>upcoming meetings</a> to find out when the first one will be!</p>
<% else %>
  <table>
    <thead>
      <tr>
        <th>Date</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @meetings.each do |meeting| %>
        <tr>
          <td><%= meeting.date %></td>
          <td><%= link_to 'Show', meeting %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

<br>