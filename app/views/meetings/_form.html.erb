<%= form_for(@meeting) do |f| %>
  <% if @meeting.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(@meeting.errors.count, "error") %> prohibited this meeting from being saved:</h3>

      <ul>
      <% @meeting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field form-group">
      <%= f.label :date %>
      <%= f.date_field :date, class: 'form-control' %>
    </div><br>
    <div class="field form-group">
      <%= f.label :time %>
      <%= f.time_field :time, class: 'form-control' %>
    </div><br>
    <div class="field form-group">
      <%= f.label :description %>
      <%= f.text_area :description, height: '6', class: 'form-control' %>
    </div><br><br>
    <tr class="actions">
      <td colspan='2'><%= f.submit class: 'btn btn-success' %>
    </div>
<% end %>
